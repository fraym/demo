type BankAccount
    @crudType
    @permission(
        update: [ACCOUNTS_WRITE]
        create: [ACCOUNTS_WRITE]
        delete: [ACCOUNTS_WRITE]
        all: [ACCOUNTS_READ]
    ) {
    userId: ID! @index @filterFromJwtData(key: "user")
    iban: String!
}
